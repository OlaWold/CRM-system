spring.application.name=CRMProject
spring.datasource.url=jdbc:postgresql://localhost:5433/crm_project
spring.datasource.username=sa
spring.datasource.password=DetteKanEndres2026!

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.data.rest.base-path=/api
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

ollama.base-url=${OLLAMA_BASE_URL:http://localhost:11434}
ollama.model=${OLLAMA_MODEL:llama3.2}

mail.enabled=${MAIL_ENABLED:false}
mail.host=${MAIL_HOST:}
mail.port=${MAIL_PORT:993}
mail.user=${MAIL_USER:}
mail.password=${MAIL_PASSWORD:}
mail.folder=${MAIL_FOLDER:INBOX}
mail.poll-interval-ms=${MAIL_POLL_INTERVAL_MS:60000}